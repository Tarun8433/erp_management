Explain how this works: $ARGUMENTS

Read the specified file or feature, then give a clear explanation at two levels:

### What it does (2-3 sentences)
Plain English summary of the purpose and behaviour.

### How it works (step by step)
Walk through the code flow from entry point to output. For backend: request → middleware → controller → DB → response. For Flutter: user action → controller → API call → state update → UI rebuild.

### Key decisions
List 2-3 non-obvious implementation choices and why they were made (based on what the code shows, not guessing).

### Where to look next
If someone wants to extend or modify this, which files are the right entry points?

Keep the explanation concise. Use code snippets only to illustrate specific points, not to repeat the whole file.
